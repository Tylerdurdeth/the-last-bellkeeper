# The Last Bellkeeper v2 — lead handover (written 24 Sep 2026, 23:45 UTC)

You are the new LEAD for The Last Bellkeeper, a Three.js browser adventure for the 404 game jam (rules: /Users/seb/Documents/ChatGPT/Project BellKeeper/handover-opus-5.5-2026-09-24/references/jam/README.md). The owner (user) is nontechnical, wants implementation not plans, and has a very high visual bar. Read this whole file, then REDESIGN.md, STYLE-LOCK-V2.md and BEAUTY-PASS.md in this repo, and the memory index at /Users/seb/.claude/projects/-Users-seb-Documents-ChatGPT-Project-BellKeeper-handover-opus-5-5-2026-09-24/memory/MEMORY.md.


> 25 Sep 05:45 UTC: the scheduled relaunch stalled overnight on an unapproved tool prompt (unattended run), so NO work happened between 23:46 and 05:45 UTC. The scheduled task `bellkeeper-lead-relaunch` is now DISABLED; the owner starts attended lead sessions manually. ~12 h remain to the 18:00 UTC freeze — prioritise: (1) camera collapse in the gate yard, (2) updraft exit assist, (3) tagline + charge priority + catch radius + fragment hint, (4) villagers (rebuild with CC0 UAL animation or remove), Mara chin, hero coat dither, (5) ghost walls, (6) fresh full-game review, (7) publish v2 per OWNER DECISIONS, (8) entry materials.

## OWNER DECISIONS (re-read this section at every checkpoint; newest first)
- 25 Sep 2026 ~00:00 UTC — NPC animation: YES, download the free CC0 Quaternius Universal Animation Library (Standard edition, https://quaternius.itch.io/universal-animation-library) and use its clips (idles, talk, wave, sit, …) for Mara and the villagers via the existing retarget path (animation tracks only; update PROVENANCE/licence; declare CC0 in the entry).
- 25 Sep 2026 ~00:00 UTC — Publishing: YES, publish v2 to the public site (main → GitHub Pages) AFTER a fresh first-exposure review confirms v2 is better than the live version AND full keyboard+touch routes and the jam gate pass; then verify published bytes and run the LIVE gate. No separate approval needed after that.

## Deadlines (UTC)
- Game-change FREEZE: 25 Sep 18:00. After that: only publishing, live verification and entry materials.
- Official jam close: 25 Sep 23:59 (PR must be open). Do NOT submit the PR, accept terms or message organisers — prepare materials and ask the owner.
- Always check the real time with `date -u` before stating it. (The previous lead mis-estimated times; the owner noticed.)

## Where things are
- Repo: /Users/seb/Documents/Codex/the-last-bellkeeper, branch `v2-redesign` (last checkpoint fd49f8b + this file). Public site (GitHub Pages from `main`, docs/) still serves the v1 safety-net release 40de78a: https://tylerdurdeth.github.io/the-last-bellkeeper/
- Dev server: http://127.0.0.1:4173/the-last-bellkeeper/ serves repo docs/ (`node tools/serve.mjs` from the repo; restart if down). docs/ is generated by `node tools/build.mjs` — never edit docs/ by hand; never ship docs/assets/moth.js or mushroom.js (unapproved; untracked).
- Owner's STABLE PREVIEW: git worktree /Users/seb/Documents/Codex/bellkeeper-preview served on http://127.0.0.1:4176/the-last-bellkeeper/ via `node serve-4176.mjs` (run from that worktree). Refresh it only with tested commits: commit docs built from committed code, then `git -C /Users/seb/Documents/Codex/bellkeeper-preview checkout -q --detach <commit>`; restart the server if it isn't responding; tell the owner.
- Evidence (private, never publish wholesale): evidence/. Reviews: evidence/review-2026-09-24/{A,B,A2,B2,A3-story-gameplay,B3-visuals-detail,C1-critic}/REPORT.md.
- Atlas references (private): references-v2/ (R-*.png, R-villagers.png, key/, face/). Shipping Atlas images: game/textures/v2/ (provenance .md beside them).
- Recipe harness (asset verifier, jam gate): /Users/seb/Documents/ChatGPT/Project BellKeeper/recipe-reference (harness/verify.mjs, harness/jam.mjs).

## v2 architecture (who owns what)
- main.js (host, camera spring arm, HUD, intro/finale cameras), movement.js (layered ground, updrafts, knockback, anti-trap), adventure-motion.js (poses), bellhollow/quest.js (beats, save v2 `bellkeeper-adventure-v2`, validateWorld), bellhollow/wind.js (verbs), bellhollow/adapt-world.js.
- bellhollow/world.js + kit/*.js (Bellhollow geometry, colliders, anchors; tests tools/test-bellhollow-world.mjs incl. no-pocket flood fill and hanging-anchor checks), board.js + tools/beauty-board.mjs (canonical beauty frames).
- render/look.js, toon.js, outline.js, sky.js, fx/* (cel look, ink, occluder fade, foliage, natural wind/fire/water FX, shafts).
- Characters: assets/hero-study-a.js (head/face/hair lead-owned; body/clothes via code-character.js + face-r08.webp), bh-mara.js, bh-guardian.js + bellhollow/guardian.js (3-phase well fight), bh-villager*.js + bellhollow/villagers.js, bh-paired-bells.js, bellhollow/map.js (illustrated map).
- Tests: tools/test-movement, test-v2-quest, test-bellhollow-world, test-v2-reachability, test-v2-smoke, test-v2-save, test-v2-route.mjs (full route, real input; `--touch`, `--fragments`), test-v2-camera-sweep.mjs, test-interaction-zones (v1). Jam gate: `node "<recipe>/harness/jam.mjs" http://127.0.0.1:4173/the-last-bellkeeper/ --out=evidence/...` (must PASS; ready < 20 s; last ~11–12 s, 4.5 MB).

## Owner direction (non-negotiable; see memory)
- AAA-as-possible visuals; every screenshot beautiful and coherent in the game style; small details matter; environments coherent, not contrived to fit gameplay; nothing floats (banners, chains, platforms); no mannequin-like characters.
- Cel shading + ink outlines for world/characters; wind/water/fire natural and soft in contrast.
- Hero must read clearly as a friendly teenage boy (face and hair were reworked; don't regress). Mara warm and appealing.
- The owner feels the team "is not critical enough": EVERY visual change must pass an INDEPENDENT critic (a fresh agent that didn't build it) at the real gameplay camera AND 2× close-up crops before commit; the lead must also look at frames at full size. If something can't reach the bar by freeze, REMOVE it rather than ship it (e.g. villagers).
- Report to the owner frequently in short plain-English status lines with the real UTC time; send key images with SendUserFile.

## Operating rules
- Usage limits: the owner's session limit has been hit twice with 4 concurrent Opus agents + lead. Run AT MOST 3 background agents at a time; keep their test runs lean. When a limit interrupts, checkpoint-commit on-disk work after fast tests pass, then resume.
- Heartbeat: while you are the active lead, `touch /Users/seb/Documents/Codex/the-last-bellkeeper/.lead-heartbeat` at least every 30 minutes (every time you act). (The hourly relaunch task is disabled: unattended runs stall on tool-approval prompts.)
- Subagents cannot write .md report files (sandbox): save their returned text yourself.
- Commit selectively (only files a finished worker owns); never commit other workers' in-progress files unless checkpointing after an interruption.
- Atlas (image generation, ~4 credits per 1K image): via Claude in Chrome on the owner's signed-in account, project "The Bellkeeper" (https://app.atlas.design/p/edit/6311207b-c77f-4e4b-b951-e4cec57d94e3?wid=76911b8a-91b4-4fe9-96da-7a36afc12777). Workflow: set the Text→Image node prompt via the textarea's native value setter + input event, click the node's run button (2nd button in the node header), wait ~15 s, read the output URL from the element whose computed backgroundImage contains `.png` (backend.prod-market.atlas.design/.../<uuid>.png?u=...), download with curl into references-v2/ (owner approved downloads of Atlas outputs for this redesign). Lifetime ceiling 2,000 credits; ~1,931 remained on 24 Sep morning and ~40 images (~160 credits) were generated since — re-check the billing page before more.
- Downloads of any other files need the owner's explicit permission (filename, source, size).
- Publishing: the owner authorised Pages deployment in general, and APPROVED publishing v2 once a fresh first-exposure review says v2 beats live AND full routes keyboard+touch pass AND the gate passes (see OWNER DECISIONS). Procedure: merge v2-redesign to main (fast-forward or merge commit; never rewrite history), push, wait for Pages (`gh api repos/Tylerdurdeth/the-last-bellkeeper/pages/builds/latest`), verify with tools/verify-published.mjs and the LIVE jam gate with --commit.

## Open work at handover (verify status in code before redoing — workers were interrupted mid-task)
1. Gameplay/systems (interrupted while re-running tests):
   a. Updraft exit forgiving (steering assist toward the vent's ledge + ~1 s hover; any single arrow toward the ledge without Shift lands ≥9/10) — the first loft updraft stalled a fresh tester ~30 min. World side already moved ledges within 1.1 m of columns (done).
   b. Charge priority: mandatory targets beat optional ones for the single charge.
   c. Catch radius ≥1.5 m around visible gust rings; readable rings on light cobbles.
   d. Fragment 2 hint: camera look at the fragment platform after pushing the little sail; world added a gold glint post.
   e. Replace title tagline "The tree gave us its breath. Now it needs ours." (spoils the twist); grep all pre-Hollow strings.
   f. Phone: no auto camera yaw while the joystick is held.
   g. URGENT camera collapse in the seed-wheel yard past the terrace gate (camera ~2 m above hero looking straight down): low obstacles must not shorten the arm; rise before pulling in; arm ≥4.5 m; add yard/gate/workshop/loft/market to tools/test-v2-camera-sweep.mjs.
   Then: full keyboard (--fragments) + touch routes + gate.
2. Characters (interrupted): rebuild villagers as 4 bespoke characters from references-v2/R-villagers.png (baker woman, old man with pipe and cane, girl with pinwheel, young fruit seller) at hero/Mara quality, natural proportions and poses, placed OFF paths/inlays (no bench on the boardwalk); remove Mara's "stubble" lower-face shading; fix the hero coat tails rendering dithered (register all hero parts as 'character'). If villagers can't reach the bar, remove them.
3. NPC animation: owner recommends open-source animation templates for a more AAA feel. The hero already uses the CC0 Quaternius Universal Animation Library (only 7 clips kept: game/assets/quaternius/motion.json; tools/trim-ual.py, tools/extract-motion.mjs; PROVENANCE.md). The full free pack (https://quaternius.itch.io/universal-animation-library, Standard edition, CC0) has many more clips (idles, talk, wave, sit…). The owner APPROVED re-downloading it (see OWNER DECISIONS). Then extract selected clips (animation tracks only, no meshes) and drive Mara/villagers with them via the existing retarget path; declare CC0 in the entry.
4. Critic C1 (interrupted): rerun a harsh independent close-up audit (characters, props/layout, z-fighting from 2–3 camera yaws, ghost walls, HUD) on the current build; feed its punch list to workers.
5. Render: large terrace walls near the camera still go ghostly/translucent from the occluder fade (owner screenshot) — only small props may fade; the camera should avoid sitting behind walls. Trunk bark close to camera reads as dark blocks on some terrace frames.
6. After fixes: refresh the stable preview, run a FRESH first-exposure reviewer through the WHOLE game (A4), a fresh visual critic, fix, then ask the owner about publishing v2.
7. Entry materials (by ~20:00 UTC 25 Sep): fill entries/_template.json from the jam repo (title, play link, repo, three sentences, what_i_found = "wind as a physical tool: catch/give/updraft/push/chain…", tools and models used: Claude Opus 5.5 via Claude Code, Codex/GPT for v1 history, Atlas (Gemini image) for references/textures/sky/key art, CC0 Quaternius animation, OpenAI image only in v1 history), paste the LIVE gate verdict — show the owner, don't submit.

## Owner-reported issues already fixed (don't regress)
Floating banners/chains (anchor test), stuck in chain barrier (depenetration + failsafe + pause "Return to last safe spot"), medallion noise (fade rules), wind-well z-fighting (collar is a ring, 1ca71c9), unlikable face (face-r08 + larger eyes), "sausage"/"hat" hair (sculpted clumps + back locks), dotted map (illustrated map.js), cel-shaded wind (natural FX), Hollow see-through trunk and hard light wedges, far-plane clipping.
