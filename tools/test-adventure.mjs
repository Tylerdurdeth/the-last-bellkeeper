import puppeteer from 'puppeteer';
import fs from 'node:fs/promises';
const out=process.argv[2]||'evidence/adventure/playthrough';await fs.mkdir(out,{recursive:true});
const touch=process.argv.includes('--touch'),browser=await puppeteer.launch({headless:true}),page=await browser.newPage(),client=await page.createCDPSession();let stick,stickActive=false;
const result={kind:'Known-route real-input regression; not an unfamiliar human playtest',errors:[],steps:[],started:new Date().toISOString()};
page.on('pageerror',e=>result.errors.push(e.message));page.on('response',r=>{if(r.status()>=400)result.errors.push(`${r.status()} ${r.url()}`);});
const sleep=ms=>new Promise(r=>setTimeout(r,ms));const read=()=>page.evaluate(()=>window.__GAME__);let held=[];
async function hold(keys){if(touch){if(stickActive&&!keys.length){await client.send('Input.dispatchTouchEvent',{type:'touchEnd',touchPoints:[]});stickActive=false;}return;}for(const k of held)if(!keys.includes(k))await page.keyboard.up(k);for(const k of keys)if(!held.includes(k))await page.keyboard.down(k);held=keys;}
async function steer(dx,dz,d,yaw){const sx=dx*Math.cos(yaw)-dz*Math.sin(yaw),sy=-dx*Math.sin(yaw)-dz*Math.cos(yaw),n=Math.hypot(sx,sy)||1,m=Math.min(1,Math.max(.2,d*.8));if(!stickActive){await client.send('Input.dispatchTouchEvent',{type:'touchStart',touchPoints:[{id:1,x:stick.x,y:stick.y}]});stickActive=true;}await client.send('Input.dispatchTouchEvent',{type:'touchMove',touchPoints:[{id:1,x:stick.x+sx/n*stick.r*m,y:stick.y-sy/n*stick.r*m}]});}
async function shot(name){await page.screenshot({path:`${out}/${name}.png`});}
async function go(x,z,label,tol=.38,jump=false){let stuck=0,old=await read();for(let i=0;i<260;i++){const g=await read(),dx=x-g.pos[0],dz=z-g.pos[1],d=Math.hypot(dx,dz);if(d<tol){await hold([]);await sleep(100);console.log(label,(await read()).pos);result.steps.push({label,state:await read()});return;}
 const sx=dx*Math.cos(g.cameraYaw)-dz*Math.sin(g.cameraYaw),sy=-dx*Math.sin(g.cameraYaw)-dz*Math.cos(g.cameraYaw),keys=d>1.2?['ShiftLeft']:[];if(Math.abs(sx)>.12)keys.push(sx>0?'ArrowRight':'ArrowLeft');if(Math.abs(sy)>.12)keys.push(sy>0?'ArrowUp':'ArrowDown');if(touch){if(jump&&i%8===0){await hold([]);await page.tap('#jump');}await steer(dx,dz,d,g.cameraYaw);}else{await hold(keys);if(jump&&i%8===0)await page.keyboard.press('KeyA');}await sleep(100);const n=await read();stuck=Math.hypot(n.pos[0]-old.pos[0],n.pos[1]-old.pos[1])<.012?stuck+1:0;old=n;if(stuck>20)throw Error('Blocked '+label+' '+JSON.stringify(n));}throw Error('Timeout '+label+' '+JSON.stringify(await read()));}
async function act(kind,id){const s=await read();if(s.context!==kind||(id&&s.discoveryId!==id))throw Error(`Context ${kind}/${id} expected; got ${s.context}/${s.discoveryId} at ${s.pos}`);if(touch)await page.tap('#action');else await page.keyboard.press('Space');await sleep(850);result.steps.push({label:id||kind,state:await read()});}
try{
 result.kind=touch?'Known-route real-touch full adventure in phone emulation':'Known-route real-keyboard full adventure';await page.setViewport({width:touch?390:1360,height:touch?844:900,deviceScaleFactor:1,isMobile:touch,hasTouch:touch});await page.goto('http://127.0.0.1:4173/the-last-bellkeeper/');await page.waitForFunction(()=>window.__READY__&&document.querySelector('#startb').disabled===false,{timeout:60000});await page[touch?'tap':'click']('#startb');await page[touch?'tap':'click']('#skipIntro');if(touch)stick=await page.$eval('#stick',e=>{const r=e.getBoundingClientRect();return{x:r.x+r.width/2,y:r.y+r.height/2,r:Math.min(r.width,r.height)*.36};});await shot('arrival');
 for(const p of [[-5,13],[-6.5,11]])await go(...p,'porch');await act('porch');
 for(const p of [[-4,8],[-10,5],[-15,0],[-16,-7],[-10,-10]])await go(...p,'woodland-path');
 for(const [i,x,z]of [[0,-8.8,-10.9],[1,-7.97,-11.13],[2,-7.14,-11.36]]){await go(x,z,'instrument-'+i,.2);await act('discovery','stone-'+i);}
 await go(-10,-10,'root-approach');await go(-8,-15,'root-chime');await act('chime');
 for(const p of [[-9,-19.5],[-11,-19.5],[-13.2,-17.5]])await go(...p,'garden');await act('capture');
 for(const p of [[-11,-16],[-10,-13],[-10,-10],[-16,-7],[-15,0],[-10,5],[-4,8],[0,6],[4,4],[5,3.7]])await go(...p,'wheel-return');await act('wheel');
 for(const p of [[8,4],[8,.1],[8,-3.35],[8,-6.5],[8,-10]])await go(...p,'bridge');await act('finish');await shot('sanctuary');
 for(const p of [[8,-12],[8,-17],[5,-22]])await go(...p,'rootway');await act('campaign','source');await go(8,-22,'arch-approach');await go(8,-24,'rootway-arch');await go(5,-28,'bridge-wheel');await act('campaign','bridgeWheel');await go(8,-28,'crossing-approach');await sleep(1500);await go(8,-35,'crossing');await shot('service');
 // Wait within the catch range at the end of the vent, then return its own gust.
 await go(7.1,-34.3,'vent-end');await page.waitForFunction(()=>__GAME__.discoveryId==='serviceGust',{timeout:20000});await act('campaign','serviceGust');await go(8,-36.5,'rotor');await act('campaign','service');
 for(const p of [[8,-39.3],[8,-42.2],[8,-45]])await go(...p,'root-jump',.4,true);await shot('windworks-arrival');
 await act('campaign','chamberSource');await go(8,-50.5,'chamber-neck');await go(4,-50,'inspection');await act('campaign','inspection');
 await go(4,-53,'open-chamber');await go(8,-53,'inlet-approach');await go(8,-45,'inlet');await act('campaign','chamberSource');await go(8,-53,'inlet-return');await go(0,-56,'returnWheel');await act('campaign','returnWheel');await shot('roots-cleared');
 await go(5,-56,'return-lane-approach');for(const [x,z,id] of [[5,-59,'returnVane'],[11,-59,'outwardVane']]){const target=(await read()).campaign.guardian.target;await go(target.x,target.z,'guardian-catch-'+id);await page.waitForFunction(()=>__GAME__.discoveryId==='guardianGust',{timeout:25000});await act('campaign','guardianGust');await go(x,z,id);await act('campaign',id);await shot(id);}
 await go(8,-67,'heart-bell');await act('campaign','finalBell');await shot('restoration');
 await go(15,-55,'lift-path');await go(15,-50,'lift');await act('campaign','homeLift');await act('mara-end');await shot('ending');if((await read()).score!==1)throw Error('No ending');
 await page.reload();await page.waitForFunction(()=>window.__READY__,{timeout:60000});await page[touch?'tap':'click']('#continueb');await sleep(300);if((await read()).score!==1)throw Error('Completion lost after reload');await shot('continued');result.pass=result.errors.length===0;
}catch(e){result.failure=e.message;result.pass=false;console.error(e);await shot('failure').catch(()=>{});}finally{await hold([]).catch(()=>{});result.final=await read().catch(()=>null);result.finished=new Date().toISOString();await fs.writeFile(`${out}/result.json`,JSON.stringify(result,null,2));await browser.close();console.log(JSON.stringify({pass:result.pass,failure:result.failure,errors:result.errors}));if(!result.pass)process.exitCode=1;}
