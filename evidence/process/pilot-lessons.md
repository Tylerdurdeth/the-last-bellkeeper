# Pilot lessons applied — 16 September 2026

Observed results: bounded Atlas agent stopped with budget_exhausted=true despite98 remaining, cost2, no outputs. Direct configured image node completed for5; direct two-second ElevenLabs SFX completed for9. Total16, no open reservations. Direct media download succeeded for image; Chrome blocked audio retrieval. No listening verdict.

## Prompt changes

The phrase “reference board” encouraged a page layout, title, palette labels and invented name despite a negative no-typography instruction. Do not propagate generated lettering or the name into canon. The hero silhouette/palette and tree terrace composition remain useful references. The wheel and wind channels are too small/ambiguous to model faithfully without a more focused reference.

Future object reference template: “A single isolated [object], filling80% of a square image, entirely visible on a plain warm-gray background, three-quarter view, even soft lighting. [Explicit visible geometry: shape, part counts, dimensions/proportions, attachment points.] Softly carved forms, clean painterly color blocks; ivory #E7DDC2, patinated copper #B76F48, deep woodland #183E3D, accent turquoise #62C9BC. This is an object portrait. Image contains only the object and its soft contact shadow.” Avoid title, board, sheet, captions, graphic-design language and gameplay lore. Add only object-specific constraints. Never assume a negative instruction guarantees compliance; inspect the output before downstream work.

Separate hero, environment composition and mechanism reference purposes. Reuse this existing hero reference for three independently authored geometry strategies at zero additional Atlas cost. Do not request three paid images when the requirement is three code candidates per reference.

## Execution changes

Use direct configured Atlas nodes with visible engine/duration/resolution/max quote. Reserve that maximum before one run. Set numeric inputs using real typing and verify persisted values: initial setValue did not persist audio duration. Distinguish configured maximum, reported last-run charge and account balance. No automatic retries or large batch. Preserve original attempt logs. Failed retrieval is a retrieval defect, not a reason to regenerate.

## Review gates

Verify syntax, scale, grounding, all sides and articulated hierarchy. Inspect candidate render sheets before selection. Then inspect selected hero at gameplay distance and in motion; geometry verifier success alone is not visual or animation acceptance. Geometry authoring uses 404 path B, no paid GPU or downloaded meshes.

## First implementation feedback

The reference prompt's scenic composition did not specify enough traversable architecture. First code assembly became a broad empty disk. Independent review identified this directly. Future environment work orders must explicitly describe layered garden banks, curved paired channels, route width, silhouette/negative space and playable-camera crop. Keep hero/source/target visible at the phone aspect ratio, and test muted readability. Do not spend on another image to correct a known camera, shader, UI or layout defect.

Second review lessons: a gate animation is insufficient unless its closed collision blocks the same route; successful release needs a visible travelling current and a stable pending objective until arrival; hazard displacement needs bracing/recovery; numeric geometry passes cannot detect coplanar rendered surfaces. Test these observed defects directly with real input and moving-camera captures. Keep failed reviews instead of rewriting them as passes.
