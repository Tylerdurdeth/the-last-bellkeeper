# Independent continuous-fade retest

Candidate: served working tree above `ad77449`. Exact loaded-input file hashes are in `nearbranch-retest3/input-sha256.txt`. Reused the normal keyboard-input desktop/portrait roots approach, side arc and retreat driver; no state writes. Recordings, endpoint screenshots, read-only route telemetry and contact sheets are isolated in that folder. Runs completed without page errors.

## Bounded result: pass

The severe opaque brown flash captured in retest2 is not reproduced. Compared the prior `desktop-retreat-sample16.png` with the new recording's 5.25–5.7-second interval, extracted at every encoded frame in `desktop-flash-interval.png`: the hero, path and surrounding trees remain visible throughout that inspected interval. The former almost full-screen brown surface is absent.

Desktop and portrait full-arc contact sheets sampled at 6Hz likewise show no repeat of the opaque camera-intersection slab. Matched roots endpoints expose the forward path; the farther trunks and foliage remain, so clearance has not erased the whole forest. Peripheral trunks are still large in some approach compositions, but the captured views preserve character location and route depth rather than replacing them with an unrecognizable brown surface. The full-resolution desktop approach image at 1.5 seconds also retains both hero and path.

The portrait garden endpoint retains visible boots/lower legs, responsive flowers and current loops. This targeted neighbor check does not repeat or supersede the earlier quest/gameplay review.

## Limits

These are normal-speed automated known-route recordings, not a physical phone or first-player test. The complete arcs were judged from 6Hz samples and endpoint images; only the prior flash interval received every-encoded-frame inspection. A matching script does not guarantee identical subframe positions across runs. No global no-pop guarantee, audio assessment, newly changed rock/water/cliff approval, or overall art-quality milestone is claimed.
