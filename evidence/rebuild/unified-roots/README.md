# Unified tree-base experiment and fitted contact

One original geometry candidate, recommended. Production untouched at report time. Replaces main trunk tube and six attached root tubes with one closed radial loft: unequal angular buttresses near ground converge smoothly into the existing bent upper shaft by2.25m. Preserves existing lateral branches/canopy construction, approximate footprint,14m normalization and material names. UV first coordinate follows growth height; second wraps circumference, compatible with selected bark shader.yx. Closed caps, averaged wrap seam normals, no imported mesh.

Initial60-sided version passed asset recipe but actual integration refused backdrop303480tri>300000. Kept the assertion and reduced circumference to48 within the same candidate. Final unchanged recipe verifier PASS15732tri527meshes, dimensions14.979×13.909×7.08m, normalized ground/centering accepted. Original recipe had14758tri547meshes. Final actual backdrop assertion passes; scene/device performance remains root's gate.

A is geometry only. C additionally clones only timber geometry of each placed dynamic tree, conforms its lowest vertices to analytic terrain using world-ground difference, full weight below.06localY fading to zero by1.55. Converts back through the mesh world inverse; prototypes untouched. Recomputes normals with duplicate-corner averaging because baked assets are non-indexed; retains upper normals and recomputes bounds. No collision, terrain, route or canopy changes. Backdrop instances are not individually conformed. Twenty placed-tree initialization measured38ms desktop and37.8ms phone-emulated on this host under concurrent captures; not a device benchmark. selected-world.js removes only timing console instrumentation from tested conformed-world.js.

Actual baseline/A/C desktop route to chimes and A/C portrait routes, normal keyboard input/read-only telemetry (touch start on portrait), passed with zero shader/console/page and HTTP errors after budget repair. Inspected cottage, root approach and chime desktop, chime portrait. C visibly eliminates separate inserted-wedge root/trunk joins and its low rim better follows sloping ground. Hero/action pocket and walking path stay readable. Minor endpoint/pose drift; not pixel registered. Preexisting transparent cottage overlay at root approach remains.

Limitations: buttress silhouette is still somewhat star-shaped; broad tips can look pointed or cut off. Surface remains simplified painted game geometry. Terrain fitting is a visual placement operation, not walkable root collision. Existing trunk collider unchanged; does not add step barriers. Backdrop feet not individually fitted. No claim of full artistic reference quality or physical-iPhone performance.

Files: recipe/tree.js and _verify report/sheet; base/ is copied current build, A/geometry only, C/geometry+contact, A-phone/ and C-phone/. selected-world.js and world.patch are ready for integration. SHA256tree24eb977d226099906f81ced6168b39a6b956dc6684bc59af9ac29631dfa3d114; selectedWorldc1c21ec809d55bceb5737c3068c1538f0c2d3a6cac977916e1491cc7d34b05ab. No spending or subagents.
